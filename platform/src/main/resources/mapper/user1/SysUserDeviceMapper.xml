<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.platform.user.mapper.SysUserDeviceMapper">
    <resultMap id="SysUserDeviceResultMap" type="com.ruoyi.platform.user.domain.SysUserDevice">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="deviceModel" column="device_model" />
        <result property="deviceBrand" column="device_brand" />
        <result property="deviceSystem" column="device_system" />
        <result property="devicePlatform" column="device_platform" />
        <result property="deviceVersion" column="device_version" />
        <result property="loginIp" column="login_ip" />
        <result property="loginTime" column="login_time" />
        <result property="remark" column="remark" />
        <result property="token" column="token" />
    </resultMap>

    <select id="selectSysUserDeviceById" resultMap="SysUserDeviceResultMap">
        SELECT * FROM sys_user_device WHERE id = #{id}
    </select>

    <select id="selectSysUserDeviceList" resultMap="SysUserDeviceResultMap">
        SELECT * FROM sys_user_device
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="deviceModel != null and deviceModel != ''">AND device_model = #{deviceModel}</if>
            <if test="deviceBrand != null and deviceBrand != ''">AND device_brand = #{deviceBrand}</if>
            <if test="deviceSystem != null and deviceSystem != ''">AND device_system = #{deviceSystem}</if>
            <if test="devicePlatform != null and devicePlatform != ''">AND device_platform = #{devicePlatform}</if>
            <if test="deviceVersion != null and deviceVersion != ''">AND device_version = #{deviceVersion}</if>
            <if test="loginIp != null and loginIp != ''">AND login_ip = #{loginIp}</if>
            <if test="loginTime != null">AND login_time = #{loginTime}</if>
            <if test="remark != null and remark != ''">AND remark like concat('%', #{remark}, '%')</if>
        </where>
    </select>

    <insert id="insertSysUserDevice" parameterType="com.ruoyi.platform.user.domain.SysUserDevice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user_device (
            user_id, device_model, device_brand, device_system, device_platform, device_version, login_ip, login_time, remark
        ) VALUES (
                     #{userId}, #{deviceModel}, #{deviceBrand}, #{deviceSystem}, #{devicePlatform}, #{deviceVersion}, #{loginIp}, #{loginTime}, #{remark}
                 )
    </insert>

    <update id="updateSysUserDevice" parameterType="com.ruoyi.platform.user.domain.SysUserDevice">
        UPDATE sys_user_device
        SET
            user_id = #{userId},
            device_model = #{deviceModel},
            device_brand = #{deviceBrand},
            device_system = #{deviceSystem},
            device_platform = #{devicePlatform},
            device_version = #{deviceVersion},
            login_ip = #{loginIp},
            login_time = #{loginTime},
            remark = #{remark}
        WHERE id = #{id}
    </update>

    <delete id="deleteSysUserDeviceById">
        DELETE FROM sys_user_device WHERE id = #{id}
    </delete>

    <delete id="deleteSysUserDeviceByIds">
        DELETE FROM sys_user_device WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 分类查询 -->
    <select id="selectSysUserDeviceByCategory" resultMap="SysUserDeviceResultMap">
        SELECT * FROM sys_user_device
        WHERE user_id = #{userId}
        <if test="category == 'today'">
            AND DATE(login_time) = CURDATE()
        </if>
        <if test="category == 'week'">
            AND login_time >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
        </if>
        <if test="category == 'month'">
            AND login_time >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH)
        </if>
        ORDER BY login_time DESC
    </select>

    <!-- 当前登录设备（常见以userId和IP为依据） -->
    <select id="selectCurrentDevice" resultMap="SysUserDeviceResultMap">
        SELECT * FROM sys_user_device
        WHERE user_id = #{userId} AND login_ip = #{loginIp}
        ORDER BY login_time DESC LIMIT 1
    </select>

    <!-- 设备下线 -->
    <update id="offlineDevice">
        UPDATE sys_user_device SET online = 0 WHERE id = #{id}
    </update>

    <!-- 总登录次数 -->
    <select id="totalLoginCount" resultType="int">
        SELECT COUNT(*) FROM sys_user_device WHERE user_id = #{userId}
    </select>
    <!-- 最近一个月登录次数 -->
    <select id="monthLoginCount" resultType="int">
        SELECT COUNT(*) FROM sys_user_device
        WHERE user_id = #{userId} AND login_time >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH)
    </select>
</mapper>